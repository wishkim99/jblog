<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="category">
	<select id="findById" parameterType="string" resultType="categoryvo">
		<![CDATA[
		select id, name, password from user where id=#{id }
		]]>
	</select>

	<select id="findByIdAndPassword" parameterType="map" resultType="categoryvo">
		<![CDATA[
		select id, name from user where id=#{i } and password=#{p }
		]]>
	</select>
	
	<insert id="insert" parameterType="categoryvo">
		<![CDATA[
	insert 
		   into category
		values (null, #{name}, #{description}, #{blog_id})
		]]>
	</insert>
			
</mapper>